{
  "name": "competitor-intel-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Competitor Intelligence Dashboard — single-user POC for competitive financial + news analysis",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "preview": "npm run preview --workspace frontend"
  }
}

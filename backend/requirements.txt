fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
pydantic==2.10.3
pydantic-settings==2.6.1
openpyxl==3.1.5
groq==0.13.1
tavily-python==0.5.0
sse-starlette==2.1.3
httpx==0.28.1

# Optional — PDF export. Needs native GTK/Pango libs (esp. on Windows).
# If install or import fails, /api/report falls back to returning HTML.
weasyprint==63.1
